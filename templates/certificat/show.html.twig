{% extends 'base.html.twig' %}

{% block title %}Certificat{% endblock %}

{% block body %}
    <h1>Certificat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ certificat.id }}</td>
            </tr>
            <tr>
                <th>Specialite</th>
                <td>{{ certificat.specialite }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ certificat.createdAt ? certificat.createdAt|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('certificat_index') }}">back to list</a>

    <a href="{{ path('certificat_edit', {'id': certificat.id}) }}">edit</a>

    {{ include('certificat/_delete_form.html.twig') }}
{% endblock %}
